<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.MoviesMate" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/red</item>
        <item name="colorSecondary">@color/red</item>
        <item name="colorPrimaryVariant">@color/white</item>
        <item name="android:statusBarColor">@color/red</item>
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="Theme.MoviesMate" parent="Base.Theme.MoviesMate" />

    <style name="Theme.AppCompat.Start" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/black</item>
        <item name="postSplashScreenTheme">@style/Theme.MoviesMate</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_movie</item>
    </style>
</resources>